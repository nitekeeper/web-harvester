// src/domain/templates/defaultTemplate.ts
//
// Re-exports the built-in default template from @shared/defaultTemplate so
// domain and application layer code can continue using this import path.

export { DEFAULT_TEMPLATE } from '@shared/defaultTemplate';
